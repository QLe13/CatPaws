{
	"workspaces": [
		"cspace-firebase",
		"client"
	],
	"scripts": {
		"build": "cd client && npm run build",
		"dev": "cd client && npm run dev",
		"deploy": "cd ./cspace-firebase && npm run deploy"
	},
	"devDependencies": {
		"typescript": "^5.0.2"
	}
}
